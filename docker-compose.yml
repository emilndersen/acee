version: '3.8'
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: acee
      POSTGRES_USER: acee
      POSTGRES_PASSWORD: acee
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data:
